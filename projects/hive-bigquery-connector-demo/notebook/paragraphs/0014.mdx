Now let's create the corresponding table definition in Hive. To establish the link with the BigQuery table, you need to specify `BigQueryStorageHandler` as the storage handler and provide the `bq.table` value in the table properties:
