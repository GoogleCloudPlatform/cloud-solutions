# Local Terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.backup

# Crash log files
crash.log
*.log

# Exclude all .tfvars files, which are likely to contain sensitive data,
# such as passwords or private keys.
# It is recommended to commit a .tfvars.example file instead.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local testing
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform plan files
tfplan
