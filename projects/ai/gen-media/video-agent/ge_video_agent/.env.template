# Required — GCP project and region
GOOGLE_CLOUD_LOCATION=us-central1
GOOGLE_CLOUD_PROJECT=your-project-id

# Required — GCS bucket for storing video/image artifacts
GOOGLE_CLOUD_BUCKET_ARTIFACTS=your-project-id-video-ads-artifacts

# Required — Agent version (used by adk_common)
AGENT_VERSION=1.0.0

# Optional — Override video generation model
# VIDEO_GENERATION_MODEL=veo-3.1-generate-001
